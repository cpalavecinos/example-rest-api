{
  "name": "example",
  "version": "1.0.0",
  "description": "exaple-rest-api-express",
  "main": "server.js",
  "private": true,  
  "scripts": {
    "start": "PORT=3000 && NODE_ENV=development && node server.js",
    "dev": "PORT=3000 && NODE_ENV=development && nodemon server.js",
    "prod": "PORT=3000 && NODE_ENV=production && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.16.1",
    "express": "^4.14.1",
    "express-validator": "^5.3.1",
    "jsonwebtoken": "^8.4.0",
    "log4js": "^4.0.2",
    "mongoose": "^4.8.5"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
